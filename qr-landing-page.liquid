{% comment %} This is the boilerplate for what is called a theme in shopify. This theme was made to not break any themes currently used by the client.  The line that contains {{ page.content }} is where the content of page-content would be inserted. {% endcomment %}

<div class="page-width">
  <div class="grid">
    <div class="grid__item medium-up--five-sixths medium-up--push-one-twelfth">
      <div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div>

      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>